/*wap to check no is larger numnber */

#include <stdio.h>

int main()
{
   int x,y;
   printf("enter the numbers\n");
   scanf("%d,%d",&x,&y);
   if(x>y)
   {
       printf("x is larger then y");
   }
   else
   {
       printf("y is larger then x");
   }
    return 0;
}
