/* wap to calculate area of different shape for example circle ,rectangle,triangle using function  overloading .
#include<iostream>
#define pie 3.14
using namespace std;
class area
{
	int l,w;
	int r;
	int h,b;
	public:
		area(int a,int b1)
		{
			l=a;
			w=b1;
		}
		area(int r1);
		{
			r=r1;
		}
		
}*/
/*#include<bits/stdc++.h>
using namespace std;
void seive()
{
	vector<bool>is_prime(100000001,true);
	long long n=100000001;
	vector<long long>prime;
	int c=0;
	is_prime[0]=is_prime[1]=false;
	for(long long i=2;(long long)i*i<=n;i++)
	{
		if(is_prime[i])
		{
			for(long long j=(long long)i*i;j<=n;j+=i)
			{
				is_prime[j]=false;
			}
		}
	}
	int j=0;
	for(long long i=1;i<=100000000;i++)
	{
		if(is_prime[i]==true)
		{
			prime.push_back(i);
			if(j%100==0)	cout<<prime[j]<<endl;
			j++;
		}
	}
 
}
int main()
{
	seive();
 
}*/
#include<bits/stdc++.h>
#define f first
#define s second
using namespace std;
int main()
{
    int n,a,b;
    cin>>n>>a>>b;
    vector<pair<int,int>>sc(n,0);
    if(a>b) cout<<
    for(int i=0;i<n;i++)
    {
        cout<<sc[i].f<<":"<<sc[i].s<<endl;
        if()
    }
    
}

