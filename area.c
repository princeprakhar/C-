#include<stdio.h>
main()
{
    int length,base,area;
    printf("enter the length");
    scanf("%d",&length);

    printf("\n enter the base ");
    scanf("%d",&base);

    area=length*base;

    printf("\n area of reactangle is %d",area);
    getch();
}
